<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mymapper">
    <select id="getUserById" resultType="org.example.mybatis.model.User">
        select * from user where id=#{id};
    </select>

    <select id="getAllUser" resultType="org.example.mybatis.model.User">
        select * from user;
    </select>

    <insert id="addUser" parameterType="org.example.mybatis.model.User">
        insert into user (username, address) values (#{username}, #{address});
    </insert>

    <insert id="addUser2" parameterType="org.example.mybatis.model.User">
        insert into user (id, username, address) values (#{id}, #{username}, #{address});
    </insert>

    <delete id="deleteUserById" parameterType="java.lang.Integer">
        delete from user where id = #{id}
    </delete>

    <update id="updateUser" parameterType="org.example.mybatis.model.User">
        update user set username = #{username} where id = #{id}
    </update>
</mapper>